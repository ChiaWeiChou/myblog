# 儀表板顯示問題修正報告

## 修正日期
2026-03-18

## 修正人員
鳴人（第七班執行與產出）

---

## 問題總覽

### 問題1：每日市場摘要顯示不一致
- **現象**：03/18、03/16、03/15 的「主要發展」只有簡短標題，缺少詳細描述；03/17 顯示正確（完整的事件背景、詳細描述、各方反應等）
- **根因**：資料庫中部分日期的 `key_events` JSON 欄位，`description` 只包含「來源: XXX」這樣的簡短文字，缺少詳細描述

### 問題2：價格卡片漲跌幅不一致
- **現象**：Bitcoin 卡片顯示「📈 +5.3%」，但 24h 變化卻是「+0.34%」
- **根因**：價格異動提醒徽章（price alert badge）計算的是與24小時前快取價格的差異，而不是使用 API 返回的 24h 變化

---

## 修正內容

### 1. 主要發展顯示改善（index.html）

**修改位置**：`renderSummaryCard` 函數中的 `developmentsHtml` 生成邏輯

**修改前**：
```javascript
const developmentsHtml = keyEvents.map((event, index) => {
    const title = event.title || `事件 ${index + 1}`;
    const desc = event.description || '';
    const source = event.source || '';
    const impact = event.impact_scope || '';
    // ...
}).join('');
```

**修改後**：
```javascript
const developmentsHtml = keyEvents.map((event, index) => {
    const title = event.title || `事件 ${index + 1}`;
    let desc = event.description || '';
    let source = event.source || '';
    const impact = event.impact_scope || '';
    
    // 處理描述欄位只有「來源: XXX」的情況
    if (desc && desc.startsWith('來源:')) {
        // 如果描述只有來源資訊，將其移到 source 欄位
        if (!source) {
            source = desc.replace('來源:', '').trim();
        }
        desc = ''; // 清空描述，因為它只包含來源資訊
    }
    // ...
}).join('');
```

**效果**：當 `description` 欄位只包含「來源: XXX」時，會將其移到 📰 標籤中顯示，不再重複顯示在描述區域

---

### 2. 價格卡片漲跌幅一致性修正（index.html）

**修改位置**：`renderPrices` 函數中的價格異動提醒徽章生成邏輯

**修改前**：
```javascript
// 計算價格異動提醒
const alert = calculatePriceAlert(item.price, item.symbol);
const alertBadge = alert.show ? `
    <div class="price-alert-badge absolute -top-2 -right-2 ${alert.isUp ? 'bg-green-500' : 'bg-red-500'} text-white text-xs font-bold px-2 py-1 rounded-full shadow-lg">
        ${alert.isUp ? '📈' : '📉'} ${alert.isUp ? '+' : ''}${alert.percent.toFixed(1)}%
    </div>
` : '';
```

**修改後**：
```javascript
// 計算價格異動提醒 - 使用與卡片正面相同的 24h 漲跌幅
const changePercent = item.change_24h_percent || 0;
const alertBadge = Math.abs(changePercent) >= 5 ? `
    <div class="price-alert-badge absolute -top-2 -right-2 ${changePercent >= 0 ? 'bg-green-500' : 'bg-red-500'} text-white text-xs font-bold px-2 py-1 rounded-full shadow-lg">
        ${changePercent >= 0 ? '📈' : '📉'} ${changePercent >= 0 ? '+' : ''}${changePercent.toFixed(1)}%
    </div>
` : '';
```

**效果**：價格異動提醒徽章現在直接使用 API 返回的 `change_24h_percent`，與卡片正面顯示的 24h 變化一致

---

### 3. 趨勢圖漲跌幅一致性修正（index.html）

**修改位置**：`calculateChartStats` 函數

**修改前**：
```javascript
function calculateChartStats(symbol, data, days) {
    // ...
    // 使用 API 返回的 24h 漲跌幅，與卡片正面保持一致
    const latestData = data[0]; // 最新數據在第一筆
    const changePercent = latestData?.change_24h_percent || 0;
    // ...
}
```

**修改後**：
```javascript
function calculateChartStats(symbol, data, days) {
    // ...
    // 使用與卡片正面相同的 24h 漲跌幅（從 priceData 全域變數獲取）
    const changePercent = priceData[symbol]?.change_24h_percent || 0;
    // ...
}
```

**效果**：趨勢圖統計數據中的「漲跌幅」現在與卡片正面顯示的 24h 變化一致

---

## 驗證結果

### 語法驗證
```bash
$ node --check /tmp/dashboard_script.js
JavaScript 語法檢查通過！
```

### 瀏覽器驗證

#### 驗證1：Bitcoin 漲跌幅一致性
- **卡片正面**：顯示「+0.34%」
- **趨勢圖統計**：顯示「漲跌幅 +0.34%」
- **結果**：✅ 一致

#### 驗證2：主要發展顯示格式
- **03/18 主要發展**：來源資訊正確顯示在 📰 標籤中
- **結果**：✅ 格式統一

#### 驗證3：價格異動提醒徽章
- **Bitcoin**：漲跌幅 0.34% < 5%，不顯示提醒徽章
- **結果**：✅ 邏輯正確（只有當 |漲跌幅| >= 5% 時才顯示）

---

## 截圖證明

1. **修正前截圖**：`browser_61f0758c-d1e5-4aef-9362-a7aeedf05939.jpg`
2. **修正後截圖**：`browser_c64aef69-5c51-45fa-8b45-83cf2e2f6870.jpg`

---

## 後續建議

1. **資料品質改善**：建議檢查資料生成流程，確保所有日期的 `key_events` 都有完整的 `description` 和 `source` 欄位
2. **資料庫修正**：可以考慮更新歷史資料的 `key_events` JSON，補齊缺少的詳細描述

---

## 檔案修改記錄

| 檔案 | 修改內容 |
|------|----------|
| `/Users/kaui/.openclaw/workspace/dashboard/index.html` | 1. 改善主要發展顯示邏輯<br>2. 修正價格異動提醒徽章計算方式<br>3. 修正趨勢圖漲跌幅計算方式 |

---

**報告完成時間**：2026-03-18 07:10 GMT+8
